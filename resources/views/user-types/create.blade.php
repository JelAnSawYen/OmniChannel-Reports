@extends('layouts.app')
@section('content')
<div class="page-head"><div><h1 class="page-title">Add User Type</h1><p class="page-subtitle">Create an access role and choose the permissions it can use.</p></div></div>
<div class="panel form-card"><form method="POST" action="{{ route('user-types.store') }}">@csrf<div class="form-group"><label>User Type Name</label><input class="form-control" name="name" value="{{ old('name') }}" required></div><div class="form-group" style="margin-top:16px"><label>Description</label><textarea class="form-control" name="description">{{ old('description') }}</textarea></div><div class="form-group" style="margin-top:16px"><label>Permissions</label><div class="permission-grid">@foreach($permissions as $key=>$label)<label class="permission-item"><input type="checkbox" name="permissions[]" value="{{ $key }}" {{ in_array($key,old('permissions',[]),true)?'checked':'' }}><span><strong>{{ $label }}</strong><small class="muted" style="display:block">{{ $key }}</small></span></label>@endforeach</div></div><div class="form-actions"><a class="btn secondary" href="{{ route('user-types.index') }}">Cancel</a><button class="btn primary">Save</button></div></form></div>
@endsection
